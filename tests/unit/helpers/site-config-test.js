import { siteConfig } from 'goal-sentry/helpers/site-config';
import { module, test } from 'qunit';

module('Unit | Helper | site config');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = siteConfig([42]);
  assert.ok(result);
});
